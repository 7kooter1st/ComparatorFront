# Базовый URL REST API (по умолчанию /api — проксируется на localhost:5000)
# VITE_API_BASE=http://localhost:5000/api

# WebSocket base (по умолчанию текущий хост — прокси /ws на localhost:5000)
# VITE_WS_BASE=ws://localhost:5000
